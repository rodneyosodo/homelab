services:
  swagger-editor:
    container_name: swagger-editor
    image: docker.io/swaggerapi/swagger-editor:v5.7.0
    restart: unless-stopped
    networks:
      - homelab-network
    security_opt:
      - no-new-privileges:true
    ports:
      - 2080:80
